/** @format */

import React from 'react'
import { PostByMainIdWithSubMenusAndItems } from 'src/types'

type Props = {
  mainMenuContent: PostByMainIdWithSubMenusAndItems
}

export default function MainContentComponent({ mainMenuContent }: Props) {
  return (
    <React.Fragment>
      <></>
    </React.Fragment>
  )
}
